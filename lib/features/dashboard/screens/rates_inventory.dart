import 'package:flutter/material.dart';

class RatesInventory extends StatelessWidget {
  const RatesInventory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}