import 'package:flutter/material.dart';

class QuickReservation extends StatelessWidget {
  const QuickReservation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}