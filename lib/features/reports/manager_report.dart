import 'package:flutter/material.dart';

class ManagerReport extends StatelessWidget {
  const ManagerReport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
