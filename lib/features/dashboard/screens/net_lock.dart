import 'package:flutter/material.dart';

class NetLock extends StatelessWidget {
  const NetLock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
