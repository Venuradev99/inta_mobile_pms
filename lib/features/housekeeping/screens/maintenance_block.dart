import 'package:flutter/material.dart';

class MaintenanceBlock extends StatelessWidget {
  const MaintenanceBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
